# Services

This folder is the standard home for first-party backend services.

- `ml-backend/` will eventually hold the disease prediction API.
- `health-notices/` will eventually hold the weather-based health notices service.
